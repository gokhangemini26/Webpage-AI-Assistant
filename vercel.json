{
    "version": 2,
    "buildCommand": "if [ -f packages/database/schema.prisma ]; then npx prisma@5 generate --schema=packages/database/schema.prisma; else npx prisma@5 generate --schema=../../packages/database/schema.prisma; fi && (npm run build:web || npm run build)",
    "outputDirectory": "apps/web/dist",
    "installCommand": "npm install",
    "rewrites": [
        { "source": "/api/(.*)", "destination": "/api" },
        { "source": "/(.*)", "destination": "/index.html" }
    ]
}
